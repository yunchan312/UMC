import { useSetRecoilState, atom } from "recoil";
import tw from "tailwind-styled-components";
import { useState } from "react";
import { modalShown } from "./atoms";

export default function Modal(isSubmit) {
  return <div></div>;
}
